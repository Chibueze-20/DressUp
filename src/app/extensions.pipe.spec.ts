import { ExtensionsPipe } from './extensions.pipe';

describe('ExtensionsPipe', () => {
  it('create an instance', () => {
    const pipe = new ExtensionsPipe();
    expect(pipe).toBeTruthy();
  });
});
